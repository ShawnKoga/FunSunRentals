import React, { Component } from 'react';

export default class Add_Inventory extends Component {
    render(){
        return (
            <div>
                ADD INVENTORY PAGE
            </div>
        )
    }
}